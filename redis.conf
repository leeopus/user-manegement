# Redis 安全加固配置
# 密码通过命令行 --requirepass 传入

# 持久化
appendonly yes

# 内存限制
maxmemory 512mb
maxmemory-policy allkeys-lru

# 禁用危险命令
rename-command FLUSHDB ""
rename-command FLUSHALL ""
rename-command DEBUG ""

# 网络（Docker 内网通信，密码通过 --requirepass 传入）
bind 0.0.0.0

# 超时
timeout 300
tcp-keepalive 60
